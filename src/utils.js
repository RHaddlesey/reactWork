console.log('my utils are running');

const square = (x) => x * x;
const add = (a, b) => a + b;
const minus = (a, b) => a - b;

export { square, add, minus };

// 2 types of export - default and named 